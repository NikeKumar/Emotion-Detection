check python is installed or not (" python --version ")
if python is not installed, install it from the browser

then extracting the Emotion Detection.zip

after extracting....

open cmd

goto the project location (D:Project/Emotion Detection/)

type the command " pip install -r requirements.txt "

after installing required libraries

type " python app.py "

then it gives a server and address (eg: http://127.0.0.1:5000/)

copy and paste it in the browser

Enjoy with the result....:)